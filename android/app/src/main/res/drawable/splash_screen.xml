<?xml version="1.0" encoding="utf-8"?>
<!-- Full-screen branded cold-start background: the exact JS splash design
     (warm radial gradient, ripple rings, cream disc, Legaxy wordmark) so the
     native launch matches the in-app splash. Stretched to fill the window. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:src="@drawable/splash_full"
            android:gravity="fill" />
    </item>
</layer-list>
